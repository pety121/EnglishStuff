### 牛津3000核心词表by Level提取json与清洗规范指南

**1. 数据来源与获取方式**
本项目的原始数据来源于牛津官方 PDF 词表。为确保数据提取的准确性，需将 PDF 文件在浏览器中打开，使用“全选”功能复制页面内容，并将其粘贴保存为纯文本（TXT）文件作为后续处理的唯一数据源。

**2. 核心处理逻辑（状态机模式）**
文本清洗需严格按照以下流水线逐行执行：

- **全局清洗**：首先定位文本中首个 `A1` 标识，将其之前的所有前言、版权信息全部替换为空；随后使用正则表达式，将所有包含 ` Oxford University Press` 及页码（如 `数字 / 12`）的页眉页脚内容全部替换为空。
- **区块识别与缓存**：逐行扫描文本，当识别到 `A1`、`A2`、`B1`、`B2` 等区块标题时，切换当前工作缓存区。后续提取的单词将自动归入对应的区块列表中。若遇到文件末尾，即使无后续区块标识也正常结束。
- **词性过滤与单词提取**：对于缓存区内的每一行，需先判断其是否为词性标识。若行首匹配标准词性缩写（如 `n.`、`v.`、`adj.`）、带数字的词性（如 `2 v.`）或特殊词性白名单（如 `modal`、`auxiliary`、`number`），则判定为词性行并直接丢弃。若未被判定为词性，则视为有效单词或短语，提取其行首的英文字母部分，并剔除末尾的冗余数字（如将 `can 1` 还原为 `can`），最终存入当前区块并自动去重。

**3. 官方词性标识参考字典(可网络搜索,官方有对应的说明)**
为防止机器误判，提取脚本需内置以下基于牛津官方词典体例的词性标识规则：

- **常规词性（带缩写点）**：`n.`（名词）, `v.`（动词）, `adj.`（形容词）, `adv.`（副词）, `prep.`（介词）, `conj.`（连词）, `pron.`（代词）, `det.`（限定词）, `exclam.`（感叹词）。
- **特殊词性（无缩写点，需加入白名单）**：`modal`（情态动词）, `auxiliary`（助动词）, `number`（数词）, `abbrev.`（缩写）。
- **短语标识**：多词组成的固定搭配（如 `according to`），其后通常跟随 `prep.` 或 `conj.`，需作为完整字符串提取。

**4. 扩展性维护提示**
本规范已涵盖当前版本牛津3000词表的所有已知排版特征。若未来官方词表更新，新增了词性缩写或改变了排版格式，只需在“特殊词性白名单”或“常规词性正则”中补充相应标识即可，核心提取逻辑无需重构。
